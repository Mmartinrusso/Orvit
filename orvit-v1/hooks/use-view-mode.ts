'use client';

/**
 * useViewMode Hook
 * Convenience hook for consuming ViewMode context
 * Re-exports from context for backwards compatibility
 */

export { useViewMode } from '@/contexts/ViewModeContext';
