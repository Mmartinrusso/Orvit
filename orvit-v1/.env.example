# =============================================================================
# ORVIT - Variables de Entorno (.env.example)
# =============================================================================
# IMPORTANTE: Este archivo es SEGURO para commitear al repositorio.
# Copia este archivo como .env y reemplaza los valores con tus credenciales reales.
# NUNCA commitees el archivo .env con valores reales.
# =============================================================================

# --- Base de datos (PostgreSQL / Neon) ---
DATABASE_URL=postgresql://user:password@host:5432/database?sslmode=require
DIRECT_URL=postgresql://user:password@host:5432/database?sslmode=require

# Base de datos secundaria (T2) - opcional
DATABASE_URL_T2=postgresql://user:password@host:5432/database_t2?sslmode=require
DIRECT_URL_T2=postgresql://user:password@host:5432/database_t2?sslmode=require

# --- Autenticación ---
# CRÍTICO: Debe tener al menos 32 caracteres. Generar con:
#   node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=your-jwt-secret-here-must-be-at-least-32-characters-long

# Secret interno para endpoints del scheduler (auto-reset de tareas fijas).
# Generar con: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
INTERNAL_API_SECRET=your-internal-api-secret-here

# --- AWS S3 (Almacenamiento de archivos) ---
# NUNCA usar NEXT_PUBLIC_ para variables AWS
AWS_ACCESS_KEY_ID=your-aws-access-key-id
AWS_SECRET_ACCESS_KEY=your-aws-secret-access-key
AWS_REGION=us-east-2
AWS_S3_BUCKET=your-bucket-name

# --- OpenAI ---
OPENAI_API_KEY=sk-your-openai-api-key

# --- Discord (Bot / Notificaciones) ---
DISCORD_BOT_TOKEN=your-discord-bot-token
DISCORD_GUILD_ID=your-discord-guild-id

# --- Sentry (Error Tracking) ---
SENTRY_AUTH_TOKEN=your-sentry-auth-token
SENTRY_ORG_SLUG=your-org
SENTRY_PROJECT_SLUG=your-project

# --- WhatsApp ---
WHATSAPP_API_URL=https://your-whatsapp-api-url

# --- Entorno ---
NODE_ENV=development
