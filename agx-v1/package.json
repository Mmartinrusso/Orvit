{
  "name": "agx-code",
  "version": "2.0.0",
  "type": "module",
  "description": "AGX — AI Development Tool powered by Claude",
  "main": "dist/index.js",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc",
    "start": "node dist/index.js",
    "typecheck": "tsc --noEmit",
    "install:client": "cd client && npm install",
    "build:client": "cd client && npm run build",
    "setup": "npm install && npm run install:client && npm run build:client",
    "dev:client": "cd client && npm run dev"
  },
  "dependencies": {
    "@fastify/static": "^8.3.0",
    "dotenv": "^16.4.7",
    "fastify": "^5.2.1",
    "pg": "^8.18.0",
    "pino": "^9.6.0",
    "pino-pretty": "^13.0.0",
    "uuid": "^11.0.5",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "@jest/globals": "^30.2.0",
    "@types/node": "^22.10.7",
    "@types/pg": "^8.16.0",
    "@types/uuid": "^10.0.0",
    "tsx": "^4.19.2",
    "typescript": "^5.7.3"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
