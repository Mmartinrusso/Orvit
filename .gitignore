# Dependencies
node_modules/
.pnp
.pnp.js

# Build outputs
dist/
.next/
out/
build/
*.tsbuildinfo

# Environment files (secrets - NUNCA commitear)
.env
.env.local
.env.*.local
.env.production
.env.staging
# NOTA: .env.example es SEGURO para commitear (solo tiene placeholders)

# IDE
.vscode/
.idea/
*.swp
*.swo

# OS files
Thumbs.db
.DS_Store
nul

# Temp/debug files
tmp_tasks.json
C:UsersUsuarioDesktopMawirtmp_tasks.json
*.log

# Claude local settings (per-machine)
.claude/settings.local.json
.claude/plans/
.claude/projects/

# Package lock (keep package-lock.json but ignore others)
# yarn.lock - keep if using yarn

# Coverage
coverage/
.nyc_output/

# AGX — herramienta de desarrollo interna, no va a producción
agx-v1/

# Tests standalone del root (no son tests de orvit-v1)
tests/

# Archivos temporales / artefactos locales
IndirectViewV2_err.txt
plan.md
*.txt
